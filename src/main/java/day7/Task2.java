package day7;

public class Task2 {
    public static void main(String[] args) {
        // Player p1 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p2 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p3 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p4 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p5 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p6 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));
        // Player p7 = new Player((int) (Math.random() * (Player.MAX_STAMINA - Player.MIN_STAMINA) + Player.MIN_STAMINA));

        // while (p1.getStamina() > 0) {
        //     p1.run();
        // }
        // System.out.println((Player.getCountPlayers()));
    }
}
